.env
.env.*
!.env.example
.DS_Store
__pycache__
.idea
KMS
.venv
.vscode
*.egg-info
.pytest_cache
.ruff_cache

# Claude Code
.claude/settings.local.json
.claude/worktrees/

# OpenAI Codex
.codex/config.local.toml

# Gemini CLI
.gemini/history/
.gemini/tmp/
.gemini/google_accounts.json
.gemini/installation_id
.gemini/oauth_creds.json

# Cursor
.cursor/chat/
.cursor/rules/*.local.mdc

# GitHub CLI
.github/personal-instructions.md

# Audition clips are large and regenerable.
audition/

# Caller memory (SQLite) — never commit real caller data
# Excluding the directory itself would stop git descending into it, and the
# negation below would never be considered. Exclude the contents instead.
data/*

# Reference data ships with the app; caller records never do.
!data/facilities.json
